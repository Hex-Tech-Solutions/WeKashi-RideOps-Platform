# Deploy trigger test

Scratch file used to trigger the `Deploy to Server` workflow on a push to `main`
while the deployment pipeline is being verified end to end.

Safe to delete once deploys are confirmed working.
