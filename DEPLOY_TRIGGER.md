# Deploy trigger test

Scratch file used to trigger the `Deploy to Server` GitHub Actions workflow on a
push to `main`, so the pipeline can be observed end to end.

Safe to delete once the deployment pipeline is confirmed working.
